export const TRANSLATIONS_NAMESPACES = {
  AUTH: "auth",
  SETTNGS: "settings",
  SHARED: "shared",
} as const;

export const SUPPORTED_LANGUAGES = ["en", "sk"];

export const TRANSLATIONS_DEFAULT_NAMESPACE = TRANSLATIONS_NAMESPACES.SHARED;
